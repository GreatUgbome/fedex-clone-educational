{
  "name": "fedex-clone",
  "version": "1.0.0",
  "description": "FedEx Clone - Educational Application",
  "private": true,
  "workspaces": [
    "backend"
  ],
  "scripts": {
    "start": "cd backend && npm start",
    "dev": "cd backend && npm run dev",
    "test": "cd backend && npm test",
    "build": "cd backend && npm install"
  },
  "engines": {
    "node": "20.x"
  }
}
